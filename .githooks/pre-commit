#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
SCRIPT_PATH="$ROOT_DIR/scripts/suggest_glossary_terms.py"

if [[ ! -f "$SCRIPT_PATH" ]]; then
  exit 0
fi

python3 "$SCRIPT_PATH" || true
